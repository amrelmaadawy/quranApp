import 'package:flutter/material.dart';

class PrayersTime extends StatelessWidget {
  const PrayersTime({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('prerys Time');
  }
}