import 'package:flutter/material.dart';

class PrayerAdhkar extends StatelessWidget {
  const PrayerAdhkar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Text('prayer adhkar'),
    );
  }
}